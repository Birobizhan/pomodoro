services:
  db:
    image: postgres:17.5
    restart: always
    shm_size: 128mb
    ports:
      - '${DB_TEST_PORT:-5433}:5432'
    env_file:
      - ./.local.env
  cache-test:
    image: redis:8.2-alpine
    restart: always
    ports:
      - '6379:6379'